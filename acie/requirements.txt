# AIROS Content Intelligence Engine
# Production dependencies — deployed on Render

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# HTTP client
httpx==0.27.0

# HTML parsing and scraping
beautifulsoup4==4.12.3
feedparser==6.0.11
lxml==5.2.2

# Database — SQLAlchemy works with both SQLite (local) and PostgreSQL (Render)
sqlalchemy==2.0.35
psycopg2-binary==2.9.9

# Scheduler
schedule==1.2.2

# Browser automation — fallback scraping only, install separately on Render if needed
# playwright==1.45.0

# Type hints
typing-extensions==4.12.2
